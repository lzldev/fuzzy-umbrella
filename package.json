{
  "name": "fuzzy-umbrella",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "biome lint **/**",
    "lint:fix": "biome lint --apply **/**",
    "format": "biome format **/**",
    "format:write": "biome format --write **/**",
    "check": "biome check **/**",
    "check:write": "biome check --apply **/**"
  },
  "devDependencies": {
    "@biomejs/biome": "1.6.4",
    "@repo/typescript-config": "workspace:*",
    "prettier": "^3.2.5",
    "turbo": "latest"
  },
  "packageManager": "pnpm@9.1.1",
  "engines": {
    "node": ">=18"
  }
}
