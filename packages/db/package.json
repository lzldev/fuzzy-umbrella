{
  "name": "@artspace/db",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "scripts": {
    "dev": "tsup --watch",
    "build": "tsup",
    "with-env": "dotenv -e ../../.env --",
    "drizzle:push": "pnpm with-env pnpm drizzle-kit push:sqlite"
  },
  "devDependencies": {
    "drizzle-kit": "^0.20.14",
    "tsup": "^8.0.2",
    "dotenv-cli": "^7.4.1"
  },
  "dependencies": {
    "drizzle-orm": "^0.30.8",
    "@libsql/client": "^0.6.0",
    "dotenv": "^16.4.5",
    "zod": "^3.22.4"
  }
}
